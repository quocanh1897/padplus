---
phase: 04-real-time-collaboration
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/real-time.spec.ts
autonomous: false
requirements:
  - COLLAB-03

must_haves:
  truths:
    - "E2E test proves two users see each other's keystrokes in real-time mode"
    - "E2E test proves edits sync after disconnect/reconnect"
    - "E2E test proves mode selector shows and switches to real-time"
    - "E2E test proves Yjs state persists across page reload"
  artifacts:
    - path: "tests/real-time.spec.ts"
      provides: "Playwright E2E tests for all COLLAB-03 behaviors"
      min_lines: 80
  key_links:
    - from: "tests/real-time.spec.ts"
      to: "/ws/pads/{slug}"
      via: "Two browser contexts connecting to same pad"
      pattern: "browser.newContext"
    - from: "tests/real-time.spec.ts"
      to: "src/routes/[...slug]/+page.svelte"
      via: "Page navigation and textarea interaction"
      pattern: "page.goto.*page.locator"
---

<objective>
Write Playwright E2E tests covering all COLLAB-03 behaviors: real-time keystroke sync between two users, disconnect/reconnect resilience, mode selector integration, and persistence. Then verify the complete real-time experience visually.

Purpose: Proves the real-time collaboration feature works end-to-end. Without these tests, there is no automated verification that the WebSocket sync, editor swap, and persistence work correctly together.
Output: Comprehensive E2E test file and visual sign-off on the real-time experience.
</objective>

<execution_context>
@/Users/alenguyen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alenguyen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-collaboration/04-RESEARCH.md
@.planning/phases/04-real-time-collaboration/04-01-SUMMARY.md
@.planning/phases/04-real-time-collaboration/04-02-SUMMARY.md

<interfaces>
<!-- Testing patterns from existing test files -->

From tests/auto-merge.spec.ts (established multi-request pattern):
```typescript
import { test, expect } from '@playwright/test';
// Tests use: page.goto, page.locator, page.request.put/patch
// Conflict simulated via direct API PUT to bump server version
```

From 04-RESEARCH.md (multi-context testing strategy):
```typescript
// Two contexts, one browser -- simulates two users
const context1 = await browser.newContext();
const context2 = await browser.newContext();
const page1 = await context1.newPage();
const page2 = await context2.newPage();

// Type in page1, verify in page2
await page1.locator('textarea.editor').type('hello');
await expect(page2.locator('textarea.editor')).toHaveValue(/hello/, { timeout: 2000 });
```

From playwright.config.ts:
```typescript
// webServer starts npm run dev
// project: chromium only
// Tests at: tests/*.spec.ts
```

From CONTEXT.md mode transition behavior:
- Switching TO real-time: editor swaps without page reload
- "Real-time" appears as third option in ModeSelector dropdown
- Connection status dot: green=connected, yellow=reconnecting, red=disconnected
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playwright E2E tests for real-time collaboration</name>
  <files>tests/real-time.spec.ts</files>
  <behavior>
    - Test 1 "COLLAB-03: mode selector shows real-time option": Navigate to pad, open ModeSelector dropdown, verify "Real-time" option is visible
    - Test 2 "COLLAB-03: switching to real-time mode shows connection dot": Set mode to real-time via PATCH API, reload page, verify connection-dot element appears with green background
    - Test 3 "COLLAB-03: keystrokes sync between two users": Create two browser contexts, both navigate to same pad in real-time mode, type in context1's textarea, verify text appears in context2's textarea within 2 seconds
    - Test 4 "COLLAB-03: edits persist across page reload": Open pad in real-time mode, type content, wait for sync, reload page, verify content is still present (Yjs persistence to SQLite)
    - Test 5 "COLLAB-03: reconnect syncs edits without data loss": Open pad in real-time in two contexts, disconnect context2 (setOffline), type in context1, reconnect context2, verify context2 receives the edits
    - Test 6 "COLLAB-03: switching from real-time to LSW restores auto-save": Switch pad from real-time to last-save-wins, verify textarea is editable and auto-save resumes (save status shows after typing)
  </behavior>
  <action>
Create `tests/real-time.spec.ts` with the following tests. Use `test.describe('Real-time Collaboration', ...)` to group them.

Helper: Create a helper function `setMode(page, slug, mode)` that calls `page.request.patch('/api/pads/${slug}/mode', { data: { mode } })` to set pad mode via API (avoids UI interaction for setup).

Helper: Create a helper `createPadInRealtimeMode(browser, slug)` that:
1. Creates context + page
2. Navigates to `/${slug}` (creates pad via SSR)
3. Sets mode to 'real-time' via API
4. Reloads page to pick up the mode
5. Waits for connection dot to appear (indicates WebSocket connected)
6. Returns { context, page }

**Test 1 - Mode selector shows real-time option:**
- Navigate to `/rt-test-selector`
- Click `.mode-trigger` button to open dropdown
- Verify `.mode-option` with text "Real-time" exists

**Test 2 - Connection dot appears in real-time mode:**
- Navigate to `/rt-test-dot`, set mode to 'real-time' via API, reload
- Wait for `.connection-dot` element to be visible
- Verify connection-dot has green background (color-success)
- Verify SaveStatus is NOT visible (replaced by dot)

**Test 3 - Keystrokes sync between two users:**
- Create two browser contexts with `browser.newContext()`
- Both open `/rt-test-sync` in real-time mode
- Wait for both to show connection dot (both connected)
- In page1: focus textarea.editor, type 'hello from user 1'
- In page2: `await expect(page2.locator('textarea.editor')).toHaveValue(/hello from user 1/, { timeout: 3000 })`
- Also verify reverse: type in page2, verify in page1
- Clean up: close both contexts

**Test 4 - Persistence across reload:**
- Open `/rt-test-persist` in real-time mode
- Type 'persistent content' in textarea
- Wait 1 second (for debounced persistence to fire)
- Reload page
- Wait for connection dot (reconnected)
- Verify textarea contains 'persistent content'

**Test 5 - Reconnect syncs edits:**
- Create two contexts, both on `/rt-test-reconnect` in real-time mode
- Wait for both connected
- Set context2 offline: `await context2.setOffline(true)`
- Wait briefly for disconnect to register
- Type 'offline edit' in page1
- Set context2 back online: `await context2.setOffline(false)`
- Verify page2 textarea contains 'offline edit' within 5 seconds

**Test 6 - Switch from real-time to LSW:**
- Open `/rt-test-switch` in real-time mode, verify connection dot
- Set mode to 'last-save-wins' via API, reload
- Verify connection dot is NOT visible
- Verify SaveStatus IS visible (shows "Saved")
- Type in textarea, verify save status changes to "Unsaved changes" or "Saving..."

Use unique pad slugs per test (prefixed with `rt-test-`) to avoid interference. Each test is independent.

Timeouts: Use generous timeouts (3000-5000ms) for WebSocket operations. Real-time sync should be fast but CI environments may be slower.
  </action>
  <verify>
    <automated>cd /Users/alenguyen/Documents/notepad && npx playwright test tests/real-time.spec.ts --project=chromium 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All 6 E2E tests pass in Chromium
    - Tests cover: mode selector integration, connection dot, keystroke sync, persistence, reconnect, mode switching
    - Tests use unique slugs and clean up contexts
    - Full test suite still passes: `npx playwright test --project=chromium`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of real-time collaboration experience</name>
  <files></files>
  <action>
Human verifies the complete real-time collaboration feature works visually and feels right. All automated work (Plans 01-03 Task 1) is complete at this point.

What was built: WebSocket server with Yjs CRDT sync, RealtimeEditor with dynamic Yjs loading, ConnectionDot status indicator, ModeSelector with real-time option, E2E test suite.

Steps to verify:
1. Start dev server: `npm run dev`
2. Open two browser windows side by side, both navigate to http://localhost:5173/rt-demo
3. In Window 1: click the mode dropdown (top right), select "Real-time"
4. Verify: green dot appears in header, textarea remains with same styling
5. In Window 2: reload page -- verify green dot appears (both in real-time mode now)
6. In Window 1: type "Hello from window 1" -- verify text appears in Window 2 within a moment
7. In Window 2: type " and hello from window 2" -- verify text appears in Window 1
8. In Window 2: close the tab, wait 2 seconds, reopen http://localhost:5173/rt-demo -- verify all text is preserved
9. Test disconnect: in Window 2 DevTools Network tab, toggle "Offline" -- green dot should turn red. Toggle off -- dot turns green, missed edits sync.
10. Switch back: in Window 1, change mode to "Last-save-wins" and reload -- verify green dot disappears, save status shows "Saved", auto-save works
11. Visual check: real-time editor should look IDENTICAL to normal textarea -- same font, padding, colors. No visual difference except green dot in header.
  </action>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>User types "approved" confirming the real-time collaboration experience works correctly and looks right</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/real-time.spec.ts --project=chromium` -- all tests pass
- `npx playwright test --project=chromium` -- full suite still passes (no regressions)
- Human verification confirms real-time sync works visually between two browser windows
</verification>

<success_criteria>
All Playwright E2E tests pass, proving: two users sync keystrokes in real-time, disconnected edits merge on reconnect, mode selector integrates correctly, and Yjs state persists. Human approves the visual experience matches the distraction-free design goal.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-collaboration/04-03-SUMMARY.md`
</output>
