---
phase: 01-working-notepad
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/pad-creation.spec.ts
  - tests/auto-save.spec.ts
  - tests/conflict.spec.ts
  - tests/landing.spec.ts
  - tests/responsive.spec.ts
  - playwright.config.ts
autonomous: false
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - CORE-06
  - INFRA-01
  - INFRA-02
  - COLLAB-01
  - DASH-01

must_haves:
  truths:
    - "E2E tests verify all 10 phase requirements pass"
    - "Pad creation test: navigating to a URL creates a working editor"
    - "Auto-save test: typing, waiting, reloading shows saved content"
    - "Conflict test: stale save shows conflict banner with resolution options"
    - "Landing test: URL bar navigates to correct pad"
    - "Responsive test: layout works on mobile viewport"
    - "User has visually verified the warm aesthetic, editor experience, and conflict flow"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright configuration for E2E tests"
    - path: "tests/pad-creation.spec.ts"
      provides: "E2E test for CORE-01, CORE-02: navigate to URL, editor appears with SSR content"
    - path: "tests/auto-save.spec.ts"
      provides: "E2E test for CORE-03: type, wait, reload, content persists"
    - path: "tests/conflict.spec.ts"
      provides: "E2E test for CORE-04, COLLAB-01: stale save shows conflict banner"
    - path: "tests/landing.spec.ts"
      provides: "E2E test for DASH-01: URL bar navigation works"
    - path: "tests/responsive.spec.ts"
      provides: "E2E test for CORE-06: mobile viewport layout check"
  key_links:
    - from: "tests/*.spec.ts"
      to: "http://localhost:5173"
      via: "Playwright browser automation against dev server"
      pattern: "page.goto"
---

<objective>
Write Playwright E2E tests covering all phase requirements, run them against the dev server, and get user visual verification of the warm aesthetic and editor experience.

Purpose: Prove all 10 requirements work end-to-end, not just in isolation. The user decision explicitly requires Playwright E2E tests. The visual checkpoint confirms the warm aesthetic and UX match expectations.
Output: Passing E2E test suite and user sign-off on visual design.
</objective>

<execution_context>
@/Users/alenguyen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alenguyen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-working-notepad/01-CONTEXT.md
@.planning/phases/01-working-notepad/01-RESEARCH.md
@.planning/phases/01-working-notepad/01-01-SUMMARY.md
@.planning/phases/01-working-notepad/01-02-SUMMARY.md

<interfaces>
<!-- From Plans 01 and 02 -- routes and endpoints the tests interact with -->

Routes:
- GET / -- Landing page with URL bar
- GET /[...slug] -- Pad editor page (auto-creates pad if not exists)
- PUT /api/pads/[...slug] -- Auto-save endpoint, accepts { content: string, version: number }, returns { version, updatedAt } or 409 with { error, content, version }

UI Elements:
- Landing page: .url-bar form, .url-input input, .url-prefix span, .go-button button
- Pad page: textarea (the editor), Header with pad name and save status
- SaveStatus: displays "Saved" / "Saving..." / "Unsaved changes"
- ConflictBanner: appears on version mismatch with overwrite and copy-reload buttons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E test suite covering all phase requirements</name>
  <files>playwright.config.ts, tests/pad-creation.spec.ts, tests/auto-save.spec.ts, tests/conflict.spec.ts, tests/landing.spec.ts, tests/responsive.spec.ts</files>
  <action>
Set up Playwright and write E2E tests that verify all 10 phase requirements.

**Playwright config (`playwright.config.ts`):**
- Use the SvelteKit Playwright integration or configure manually
- Base URL: `http://localhost:5173` (or whatever the dev server uses)
- Configure `webServer` to run `npm run dev` automatically before tests
- Use Chromium only for speed (single browser is sufficient for Phase 1)
- Set reasonable timeout (30s per test)
- Screenshots on failure

**Install Playwright browsers:**
- Run `npx playwright install chromium` to install the browser binary

**Test: Pad creation (`tests/pad-creation.spec.ts`):**
Requirements covered: CORE-01, CORE-02, INFRA-01, INFRA-02
- Test 1: Navigate to `/test-pad-creation-{random}` -- expect page to load, textarea to be visible
- Test 2: Check that the textarea has content in the HTML BEFORE JavaScript hydration. Use `page.content()` to get raw HTML and verify the textarea is not empty (or verify it renders within a specific time budget, e.g., < 500ms to first contentful paint). Alternatively: disable JavaScript with `page.setJavaScriptEnabled(false)`, navigate, verify textarea is visible.
- Test 3: Navigate to a nested slug like `/test/nested/pad-{random}` -- should work the same way

**Test: Auto-save (`tests/auto-save.spec.ts`):**
Requirements covered: CORE-03
- Test 1: Navigate to `/test-autosave-{random}`, type "Hello auto-save test" in the textarea, wait 600ms (debounce + network), reload the page, verify textarea contains "Hello auto-save test"
- Test 2: Type rapidly, verify save status shows "Unsaved changes" then "Saving..." then "Saved" transitions
- Test 3: Type, wait for save, type more, wait for save -- verify final content persists after reload

**Test: Conflict handling (`tests/conflict.spec.ts`):**
Requirements covered: CORE-04, COLLAB-01
- Test 1: Navigate to `/test-conflict-{random}`, type "version A", wait for save. Then use the API directly (`page.request.put`) to save "version B" with the current version number (simulating another user). Type again in the editor to trigger a save. Expect the save to return 409. Expect the conflict banner to appear.
- Test 2: When conflict banner appears, verify it shows the overwrite button and the copy-reload button.
- Test 3: Click "Overwrite with my version" -- verify banner disappears and content is the user's version. Reload to confirm.

**Test: Landing page (`tests/landing.spec.ts`):**
Requirements covered: DASH-01
- Test 1: Navigate to `/` -- verify the URL bar input is visible and focused
- Test 2: Type "my-test-pad" in the URL bar, press Enter -- verify navigation to `/my-test-pad`
- Test 3: Verify the `padplus/` prefix is visible in the URL bar area
- Test 4: Verify PadPlus branding and tagline are visible

**Test: Responsive layout (`tests/responsive.spec.ts`):**
Requirements covered: CORE-06
- Test 1: Set viewport to mobile (375x667, iPhone SE), navigate to `/test-responsive-{random}` -- verify textarea is visible and fills the width, no horizontal scroll
- Test 2: Set viewport to mobile, navigate to `/` -- verify URL bar is usable, not clipped
- Test 3: Set viewport to tablet (768x1024) -- verify layout adjusts appropriately

**Test naming convention:** Use descriptive test names that map to requirements, e.g., `test('CORE-01: navigating to any URL path creates a working pad', ...)`

**Run the tests:**
- Execute `npx playwright test` and fix any failures
- All tests must pass before marking this task complete
  </action>
  <verify>
    <automated>cd /Users/alenguyen/Documents/notepad && npx playwright test 2>&1 | tail -20</automated>
  </verify>
  <done>All Playwright E2E tests pass. Tests cover: pad creation via URL (CORE-01), SSR content rendering (CORE-02), auto-save persistence (CORE-03), conflict detection and resolution (CORE-04, COLLAB-01), landing page URL bar (DASH-01), and responsive mobile layout (CORE-06). Infrastructure requirements (INFRA-01, INFRA-02) are implicitly verified by all tests running against the SQLite-backed single-process server.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete Phase 1 application</name>
  <action>
User visually verifies the complete Phase 1 notepad application: landing page with URL bar, pad editor with SSR text, auto-save, conflict handling, warm UI with light/dark mode, responsive layout.

Verification steps:
1. Start the dev server: `npm run dev` (if not already running)
2. Open http://localhost:5173 in your browser

**Landing page check:**
3. Verify the landing page shows PadPlus branding, tagline "Type a name, start writing", and an address-bar-style URL input with `padplus/` prefix
4. Type "my-first-note" in the URL bar and press Enter

**Pad editor check:**
5. Verify you land on a pad editor page with the pad name in the header
6. Verify the header shows: pad name, save status ("Saved"), home link, and "last-save-wins" indicator
7. Type some text -- verify save status changes to "Unsaved changes" then "Saving..." then "Saved"
8. Reload the page -- verify your text is still there (auto-save worked)

**Warm aesthetic check (CORE-05):**
9. Verify the UI feels warm and inviting: soft color tones, rounded corners, sans-serif font, comfortable padding
10. Check dark mode: switch your OS to dark mode (or use browser DevTools to emulate `prefers-color-scheme: dark`) -- verify colors adapt

**Responsive check (CORE-06):**
11. Open DevTools, toggle device toolbar, select a mobile viewport (e.g., iPhone 14) -- verify the layout works without horizontal scroll

**Conflict check (CORE-04, COLLAB-01):**
12. Open the same pad URL in two browser tabs
13. Type in tab 1, wait for save
14. Type in tab 2, wait for save attempt -- verify conflict banner appears in tab 2
15. Verify banner offers "Overwrite" and "Copy my version and reload" options
  </action>
  <verify>User confirms all checks pass or describes issues to fix.</verify>
  <done>User has approved the visual design, warm aesthetic, and functional behavior of the Phase 1 application.</done>
</task>

</tasks>

<verification>
1. `npx playwright test` -- all E2E tests pass
2. User has visually verified the application meets aesthetic and functional expectations
</verification>

<success_criteria>
- All 5 Playwright test files pass (pad-creation, auto-save, conflict, landing, responsive)
- User approves the visual design and interaction flow
- All 10 phase requirements verified either by automated tests or user inspection
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-notepad/01-03-SUMMARY.md`
</output>
