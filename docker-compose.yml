services:
  padplus:
    build: .
    ports:
      - "8462:8462"
    volumes:
      - padplus-data:/data
    environment:
      - ORIGIN=http://localhost:8462
    restart: unless-stopped

volumes:
  padplus-data:
