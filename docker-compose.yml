services:
  padplus:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - padplus-data:/data
    environment:
      - ORIGIN=http://localhost:3000
    restart: unless-stopped

volumes:
  padplus-data:
